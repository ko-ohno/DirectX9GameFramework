過去の例

今までは、DirectXのデバイスとウィンドウを二回生成していたけれど、

今度は1回にまとめる試みをしてみる


directXのデバイスには、
フルスクリーンの値(デスクトップのクライアント領域)を渡してあげて、値的にはフルスクリーンでのレンダリングをさせる。

そこで、DirectXのビューポートを変更して、

スプラッシュスクリーンのウィンドウサイズに適用をしたレンダリングをする。

これでデバイスのリロードなしにレンダリングをする。





2.FullScreenを指定して、ウィンドウサイズを変更し、imguiの描画が崩れない方法を見つける。

グラフィックスのデバイスのリロードをなしで

グラフィックスのリロードはしなくても、ImGuiのリロードすれば解決するかも？？？　



3.結果
そもそもImGuiも描画もデバイスに引っ張られるので無理だった。

だからなんちゃってスプラッシュスクリーンを作成することにする。

ShowWindow命令をされるまではWindow